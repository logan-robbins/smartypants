---
description: Clear the Smartypants diagram and its gist ledger so the next design turn can draw the graph again.
argument-hint: ""
---

Run `npx smartypants reset` in the project root. That deletes `.smartypants/design.json` and `.smartypants/ledger.json`. Leave `smartypants.config.json`. Do not ask for confirmation. Tell the user the graph is empty and the next design turn draws it again.
