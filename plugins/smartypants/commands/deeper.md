---
description: Go one level deeper on a part of the Smartypants diagram (system to components, component to modules, module to deep-dive notes).
argument-hint: <part of the diagram>
allowed-tools: Bash(npx smartypants deeper:*)
---

Run `npx smartypants deeper "$ARGUMENTS"` in the project root and report the single line it prints. If you cannot run commands here, say so and give the user that exact command to run; do not substitute another mechanism. The open canvas updates by itself. If it says Smartypants is off, tell the user to run `/smartypants` first. Do not edit any files.
